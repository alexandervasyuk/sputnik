<%= link_to 'Unfriend' , Relationship.where("follower_id = :follower_id and followed_id = :followed_id or follower_id = :followed_id and followed_id = :follower_id", {followed_id: current_user.id, follower_id: user.id})[0], method: 'delete', confirm: "Are you sure you want to unfriend this person?", class: "btn btn-danger btn-small pull-right"%>