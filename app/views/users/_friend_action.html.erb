<%if signed_in? %>
	<%if friends?(@user)%>
		<%= render 'follow_form'%>
    <%elsif my_pending?(@user)%>
    	<%= render 'my_pending_form'%>
    <%elsif other_pending?(@user)%>
    	<%= render 'other_pending_form'%>	
	<%else%>
		<%= render 'friend_form'%>
	<%end%>
<%end%>

