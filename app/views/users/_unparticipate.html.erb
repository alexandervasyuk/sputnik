<%= form_for(current_user.participations.find_by_micropost_id(feed_item.id), html: { method: :delete, remote: true}) do |f| %>
<%= f.submit "Unparticipate", class: "btn btn-small btn-primary" %> 
<% end %>